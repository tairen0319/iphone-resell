# iPhoneリペア転売アプリ (iphone-resell)

ジャンク/B級品のiPhoneをメルカリ・Yahoo!フリマで仕入れ、バッテリー・前面パネル交換で
修理してメルカリ等で売る転売事業のための管理アプリ。GitHub Pages (`tairen0319/iphone-resell`)
でホスティング。Firebase Firestore (`iphone-resell-dc263`) を共有データストアとして、
このアプリ・2つのClaude Codeスキル・ブラウザ拡張機能が連携して動く。

## 構成要素

1. **アプリ本体** (`index.html`) — ダッシュボードに「今日の仕入れおすすめ」を直接表示。
   下に在庫一覧・売上サマリーの2ページ。旧「仕入れ判断」「相場管理」ページは自動化に伴い廃止済み。
2. **`iphone-souba-update`スキル**(`~/.claude/skills/iphone-souba-update/SKILL.md`) —
   メルカリ+Yahoo!フリマの売り切れ実績から相場を再計算し、確認を取ってからFirestoreの
   `souba`コレクションを更新する。月1回程度、手動で「相場を更新して」と頼んで実行する想定
   (確認ステップがあるため定期実行はしていない)。
3. **`iphone-shiire-search`スキル**(`~/.claude/skills/iphone-shiire-search/SKILL.md`) —
   メルカリ+Yahoo!フリマの販売中商品から仕入れ候補を検索し、利益でS/A/Bランク付けして
   Firestoreの`candidates/latest`に書き込む。**毎朝7時にローカルスケジュールタスクで自動実行**
   (`iphone-shiire-search-test-local`というtaskId、Claude Desktopアプリの「もっと見る→ルーティン」
   に表示される。名前はtestのままだが本番運用中)。確認不要で自動反映する設計。
4. **ブラウザ拡張機能** (`~/Desktop/拡張機能/iphone-extension/`) — メルカリ・Yahoo!フリマの
   商品ページを開くと自動起動する仕入れ判断パネル。同じFirestoreの`souba`から相場を取得する
   ので相場更新は自動連携済み。「仕入れ確定」でアプリと同じ`zaiko`コレクションに書き込む。

## 重要な設計判断

- **クラウドのスケジュール実行(`RemoteTrigger`/ルーティン)はメルカリ・Yahoo!フリマへの
  ネットワークアクセスがegressポリシーでブロックされて使えない。** ローカルスケジュールタスク
  (`mcp__scheduled-tasks__*`、Claude Desktopアプリ内で「アプリが起動している間だけ動く」)なら
  両サイトにアクセスできる。今後この手の自動化は必ずローカルタスクで組むこと。
- ローカルタスクの権限承認はタスク単位。新しいタスクを作ると承認をやり直す可能性があるので、
  スケジュールや中身を変える時は既存タスクを`update_scheduled_task`で書き換える方が安全
  (削除して作り直さない)。
- メルカリの検索結果ページはJS実行後に商品データが現れる(単純なcurl/WebFetchでは空になる)。
  ブラウザツールが必要。Yahoo!フリマはサーバーレンダリングされた`__NEXT_DATA__`のJSONを
  持っているが、**`curl`で連続アクセスすると数回〜十数回でWAFのレート制限にブロックされる
  (実際に発生し、しばらく解除されなかった)。Yahoo!フリマも必ずブラウザツール(`navigate`)で
  アクセスすること。curlは使わない。** ブラウザツール経由なら同程度の頻度でもブロックされない
  ことを検証済み。
- **メルカリ・Yahoo!フリマともに、価格の安い順で検索すると上位が空箱・ケースのみ・
  無関係商品(iPhone関連キーワードに引っかかった全く別カテゴリの商品)で埋まることがある。**
  メルカリは検索URLに`price_min`パラメータ(相場の30%目安)を付けるとサーバー側でほぼ解決する。
  Yahoo!フリマには価格下限フィルタが存在しない(URLパラメータもUIの絞り込み機能も無い、
  キーワードに「本体」「SIMフリー」を足しても効果なし、調査済み)ので、相場の15%未満を
  クライアント側で除外しつつ、上限額を超える実機が出てくるまで最大7ページ程度
  ページングする方式にしている(ブラウザツールならブロックの心配がないため)。
- 修理対応できるのは**バッテリー交換と前面パネル(画面)交換のみ**。ジャンク・訳あり・背面破損・
  画面が映らない/起動しない・カメラ故障・充電不良は仕入れ候補から除外(対応範囲外のため)。
- 仕入れ利益の計算式(アプリ・両スキル・拡張機能で統一済み):
  ```
  利益 = 相場 − 出品価格 − バッテリー代 − (画面割れ時のみ)パネル代 − (相場×10%、メルカリ手数料)
         − 送料210円 − その他経費500円 − 付属品代330円
  ```
  バッテリー代: 13=1540円/14=1870円/15=1980円。パネル代: 13=2750円/14=2530円/15=4400円。
  ランク: 利益1万円以上=S、7000円以上=A、5000円以上=B、5000円未満は対象外。
- 仕入れ候補は検索→上位8件程度リストアップ→利益が大きい順に個別ページを開いて売り切れ・
  致命的な問題・オークション形式でないかを確認→5件確認できたら打ち切り、という2段階選定。
- 対象機種: iPhone 13/14/15の無印のみ(mini/Pro/Pro Max/Plus/SE除外)×128/256/512GB。
- **Yahoo!フリマの「〇%OFFクーポン」「PayPay最大10%対象」系の割引は自動反映しない
  (方針確定済み)。** 調査の結果、(1)「24カウントダウンボーナス」バッジは購入金額の10%を
  後日PayPay期間限定ポイントで還元する仕組みで、しかも1人あたり月上限500円相当しかなく
  実質誤差レベル、(2)商品ページに出る「クーポン適用時」の実価格引き下げはログインした
  アカウント固有(新規購入者クーポン等)でログインしないと見えない、という2種類が確認できた。
  どちらも自動検出・自動ログインは安全面・技術面で避けるべきと判断し、代わりに
  `iphone-shiire-search`スキルのステップ4.5で「通常価格では基準未達だがYahoo!フリマの
  出品価格が10%引きになれば基準を超える商品」を`candidates/latest`の`couponItems`
  (最大3件、Yahoo!フリマのみ)という別枠に書き込む方式にした。ランク付けはせず、
  実際にクーポンを持っているかどうかの判断はユーザーに委ねる。アプリ側は
  `index.html`の「🎟️ Yahoo!フリマ 10%クーポンありなら候補」セクションで表示。

## 未解決・今後の余地

- 拡張機能フォルダ(`~/Desktop/拡張機能/iphone-extension/`)はgit管理されていない。
- ローカルスケジュールタスクの実行ログを直接見る手段がない(クラウドルーティンの
  `get_run_log`に相当するものがローカルタスクには無い)。動いたかどうかはFirestoreの
  `updatedAt`を見て判断している。
